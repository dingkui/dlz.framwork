# 6.x 升级说明

当前版本为 6.7.5。版本变化请查看根目录 [CHANGELOG.md](../../CHANGELOG.md)。

本页只记录从旧版本迁移到当前版本时需要确认的行为差异；未确认的历史说明不作为兼容承诺。升级前请重点验证：

1. 核心模块与可选插件依赖是否按需拆分；
2. `ValUtil` 对非法内容会抛出 `NumberFormatException`；
3. `Json` 的严格/宽松选项及各便捷重载默认值；
4. Caller MDC 常量使用 `DlzCaller.MDC_KEY_DLZ_CALLER`。