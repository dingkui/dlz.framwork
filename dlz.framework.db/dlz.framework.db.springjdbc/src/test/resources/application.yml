server:
  port: 80021

appname: dlz-framwork-db

spring:
  profiles:
    active: test
  application:
    name: ${appname}-test
  redis:
    #数据库索引
    database: 0
    host: 192.168.1.89
    port: 6379
    password:
    #连接超时时间
    timeout: 5000

dlz:
  db:
    log:
      showResult: true
      showRunSql: false
      showKeySql: true
    jdbcSql: true