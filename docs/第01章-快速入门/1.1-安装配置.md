# 1.1 安装配置

[← 返回文档导航](../00-文档导航.md)

---

## Maven 依赖

在 `pom.xml` 中添加：

```xml
<dependency>
    <groupId>top.dlzio</groupId>
    <artifactId>dlz-kit</artifactId>
    <version>6.7.3</version>
</dependency>
```

最新版本查看：[Maven Central](https://search.maven.org/artifact/top.dlzio/dlz-kit)

---

## Gradle 依赖

```gradle
dependencies {
    implementation 'top.dlzio:dlz-kit:6.6.4'
}
```

Kotlin DSL：

```kotlin
dependencies {
    implementation("top.dlzio:dlz-kit:6.6.4")
}
```

---

## 依赖说明

dlz-kit 只有一个外部依赖：jackson-databind（2.13.0+）。

大多数 Spring Boot 项目默认已包含 Jackson，无需额外配置。若遇版本冲突，可排除：

```xml
<dependency>
    <groupId>top.dlzio</groupId>
    <artifactId>dlz-kit</artifactId>
    <version>6.7.3</version>
    <exclusions>
        <exclusion>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </exclusion>
    </exclusions>
</dependency>
```

---

## 验证安装

```java
import com.dlz.kit.json.JSONMap;

public class InstallTest {
    public static void main(String[] args) {
        JSONMap map = new JSONMap("name", "张三", "age", 25);
        System.out.println(map.getStr("name"));  // 张三
        System.out.println(map.getInt("age"));   // 25
    }
}
```

---

## 版本兼容性

| dlz-kit | JDK | 第三方运行时依赖 |
|---------|-----|------------------|
| 6.7.3   | 8+  | 仅 SLF4J API     |

---

## 常见问题

**Q: 项目没有 Jackson 怎么办？**
A: 不需要 Jackson。dlz-kit 默认使用内置 JSON；只有显式使用 Jackson 生态兼容能力时，
才按需引入 `top.dlzio:dlz-json-jackson`。

**Q: 支持哪些 JDK？**
A: JDK 8、11、17、21。

**Q: 需要配置文件吗？**
A: 不需要，开箱即用。

---

[← 返回文档导航](../00-文档导航.md) | [下一节：五分钟上手 →](1.2-五分钟上手.md)
